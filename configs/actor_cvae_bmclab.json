{
    "dataset": "BMCLab",
    "data_mode": "carepd",
    "num_folds": 6,
    "fold": 1,
    "seed": 0,
    "batch_size": 32,
    "epochs": 500,
    "lr": 0.0001,
    "lr_scheduler": "none",
    "latent_dim": 256,
    "ff_size": 1024,
    "num_layers": 8,
    "num_heads": 4,
    "dropout": 0.1,
    "num_classes": 3,
    "_losses": "rc (direct root-centred XYZ MSE) + vel×5 (direct velocity MSE). No FK chain = unattenuated gradients. lambda_rr=0 because data is already root-centred in actor_batch_from_carepd. lambda_vel=5 gives vel gradient ~2-3x stronger than rc gradient, preventing mean-pose collapse.",
    "lambda_rc": 1.0,
    "lambda_rr": 0.0,
    "lambda_kl": 1e-05,
    "lambda_rcxyz": 0.0,
    "lambda_vel": 5.0,
    "lambda_velxyz": 0.0,
    "checkpoint_dir": "./experiment_outs/actor_cvae",
    "experiment_name": "ActorCVAE",
    "wandb_project": "carepd_recon",
    "recon_gif_every_n_epochs": 50
}
